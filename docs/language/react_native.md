# React Native
## Questions
### 1. how to add :hover pseud-class on the React Native code 
React Native does not support CSS pseudo-classes like :hover natively because it doesn't use a browser DOM layout engine.
